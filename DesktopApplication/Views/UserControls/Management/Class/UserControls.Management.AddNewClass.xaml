<UserControl x:Class="DesktopApplication.Views.UserControls.UserControlsManagementAddNewClass"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:DesktopApplication.Views.UserControls"
    mc:Ignorable="d">
    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,25,75">
                <TextBlock
                    Text="Class Name"
                    FontSize="20" FontWeight="Heavy" FontFamily="{StaticResource Roboto}"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,50"/>
                <TextBox
                    x:Name="NewClassNameTextBox"
                    Width="350" Height="50"
                    FontSize="32" FontWeight="Medium" FontFamily="{StaticResource Roboto}"
                    TextAlignment="Center" HorizontalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="25,0,0,75">
                <TextBlock
                    Text="Year of Studying"
                    FontSize="20" FontWeight="Heavy" FontFamily="{StaticResource Roboto}"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,50"/>
                <TextBox
                    x:Name="NewClassYearTextBox"
                    Width="350" Height="50"
                    FontSize="32" FontWeight="Medium" FontFamily="{StaticResource Roboto}"
                    TextAlignment="Center" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
        
        <Button
            Content="Add"
            Width="200" Height="45"
            FontSize="20" FontFamily="{StaticResource Roboto}"
            Click="AddNewClass_Click" Style="{StaticResource MaterialDesignRaisedButton}"
            HorizontalAlignment="Center"/>
    </StackPanel>
</UserControl>
